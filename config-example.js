const server = "s59-br.ikariam.gameforge.com"

const config = {
    ikariam: {
        server: `https://${server}`,
        connectTimeout: 10000
    },
    headers: {
        
    }
};
module.exports = config;